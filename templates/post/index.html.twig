{% extends 'base.html.twig' %}

{% block title %}Hello Post Controller!{% endblock %}

{% block body %}

<div class=" hero ">
		<table>
			<thead>
				<th>ID</th>
				<th>Title</th>
				<th>Description</th>
				<th>Price</th>
				<th>photo</th>
			</thead>
			<tbody>
					<td>{{ post.id }}</td>
					<td>{{ post.title }}</td>
					<td>{{ post.descripstion }}</td>
					<td>{{ post.price }}</td>
					<td> {{ post.images }}</td>
                    <td>
					    <a class="btn-primary" href="{{ path('delete', {'id': post.id}) }}">delete</a>
                        <a class="btn-primary" href="{{ path('delete', {'id': post.id}) }}">update</a>

				    </td>
                    

				</tbody>
				
			</tbody>
		</div>

	</table>
</div>

    
    

            
{% endblock %}